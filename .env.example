# Copiar a .env.local y completar con tus keys
# .env.local NO se commitea (esta en .gitignore via *.local)

# Giphy API key — registrate en https://developers.giphy.com/ → Create an App
# Si no esta seteada, el boton de GIF en las tarjetas no se muestra.
VITE_GIPHY_API_KEY=
